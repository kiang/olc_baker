<?php

class radar_axis_labels
{
    // $labels : array
    public function radar_axis_labels( $labels )
    {
        $this->labels = $labels;
    }

    public function set_colour( $colour )
    {
        $this->colour = $colour;
    }
}
